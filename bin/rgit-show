#!/usr/bin/ruby -w

require 'rubygems'
require 'git'

hashes = ARGV

hashes.map! do |hash|
  object = Git::Object.find(hash)
  
  if object.nil?
    raise Git::UnknownObjectError,
      "hash #{hash} was ambiguous or doesn't exist"
  else
    object
  end
end

print hashes
