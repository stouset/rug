#!/usr/bin/ruby -w

require 'rubygems'
require 'git'

hashes = ARGV

hashes.map! do |hash|
  if o = Git::Object.find(hash)
    next o
  end
  
  raise Git::UnknownObjectError,
    "hash #{hash} was ambiguous or doesn't exist"
end

print hashes
